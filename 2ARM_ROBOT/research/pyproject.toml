[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dapier-research"
version = "0.1.0"
description = "Hardware-agnostic research contracts for DAPIER Physical AI experiments."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
